# Security remediation plan

Generated by **SecureHandoff** (AI-assisted). Each section below covers one finding that could not be fixed automatically and describes how to remediate it.

## [HIGH] h11@0.14.0: h11 accepts some malformed Chunked-Encoding bodies - `requirements.txt`

`requirements.txt` already pins `h11==0.16.0`, which is newer than the patched version `0.16.0` and resolves CVE-2025-43859 / GHSA-vqfr-h8mv-ghfj.

Next steps:
1. Confirm your lockfile/environment was rebuilt after this pin was added.
2. Redeploy or reinstall dependencies so `h11` is actually resolved to `0.16.0`.
3. If any transitive dependency pulls a different `h11`, add or keep a top-level pin to `h11==0.16.0` and regenerate the lockfile.
4. Verify with `pip show h11` or `pip freeze | grep '^h11=='` in the target environment.

## [HIGH] idna@3.4: PYSEC-2026-215 - `requirements.txt`

The finding targets `idna@3.4`, but this `requirements.txt` already specifies `idna==3.7`, which is newer than the fixed version `3.15` referenced by the scanner. To remediate safely:

1. Verify the dependency actually installed in your environment with `pip show idna` or `pip freeze | grep '^idna=='`.
2. If another lockfile, constraints file, or transitive dependency is pinning `idna` to 3.4, update that source to `idna>=3.15`.
3. Regenerate your lockfile if you use one, then reinstall dependencies.
4. Re-run the vulnerability scan to confirm the detected `idna@3.4` instance is gone.
5. If the scanner continues to report this file, check for stale scan cache or multiple requirements files in the repository, since the shown file does not contain the vulnerable version.

## [HIGH] python-multipart@0.0.6: python-multipart: Quadratic-time querystring parsing with semicolon separators causes CPU denial of service (CWE-400) - `requirements.txt`

Verify that the resolver actually installs `python-multipart==0.0.18` or newer everywhere in your deployment pipeline, since the finding targets `0.0.6` but this file already specifies `0.0.18`.

1. Confirm the lockfile or deployed environment does not still contain `python-multipart==0.0.6`.
2. Regenerate or refresh any dependency lock/constraints files if they pin an older version.
3. Reinstall dependencies in a clean environment and check the resolved version with `pip show python-multipart`.
4. If you use transitive dependency constraints, ensure none of them force a downgrade below `0.0.30`.
5. Run your test suite after dependency refresh to confirm compatibility.

If your scanner is reporting this file, it may be using stale dependency metadata; update the dependency inventory and rescan after confirming the installed version is at least `0.0.30`.

## [HIGH] urllib3@1.26.14: urllib3 allows an unbounded number of links in the decompression chain (CWE-770) - `requirements.txt`

No code change is needed for urllib3 in this file because it is already pinned to `urllib3==2.6.3`, which is newer than the patched version `2.6.0`.

To fully remediate and avoid dependency resolution drift:
1. Keep `urllib3==2.6.3` pinned in `requirements.txt`.
2. Regenerate your lockfile or dependency snapshot if you use one, so it reflects the same fixed version.
3. Reinstall dependencies in a clean environment.
4. Verify the resolved dependency tree with `pip show urllib3` or `pip freeze | grep urllib3`.
5. If your CI/CD pipeline uses cached wheels or a separate constraints file, ensure those references also resolve to `urllib3>=2.6.0`.

If the scanner is still reporting this finding, it is likely analyzing a different environment, lockfile, or transitive dependency rather than this `requirements.txt` file.

## [MEDIUM] certifi@2022.12.7: PYSEC-2023-135 - `requirements.txt`

- The finding targets `certifi==2022.12.7`, but this file already specifies `certifi==2024.7.4`, which is newer than the fixed version `2023.7.22`.
- Verify that this is the requirements file actually used by your build and deployment pipeline.
- If another lockfile or dependency manifest exists, search for `certifi==2022.12.7` or an older transitive pin and update it to at least `2023.7.22`.
- Regenerate or refresh your dependency lock/state if needed so the deployed environment resolves to the newer certifi release.
- Re-run the vulnerability scan after the dependency resolution step to confirm the finding is cleared.

## [MEDIUM] Jinja2@3.1.2: Jinja has a sandbox breakout through malicious filenames (CWE-150) - `requirements.txt`

No automated fix could be generated. Jinja has a sandbox breakout through malicious filenames (CVE-2024-56201, PYSEC-2026-1472)

## [MEDIUM] Jinja2@3.1.2: Jinja vulnerable to HTML attribute injection when passing user input as keys to xmlattr filter (CWE-79) - `requirements.txt`

Keep `Jinja2==3.1.6` as-is; it is already patched for CVE-2024-22195 and newer than the affected `3.1.2` release. If this finding came from stale dependency metadata or another lockfile, update that source of truth to `Jinja2>=3.1.3` (or retain `3.1.6`) and regenerate any lockfiles/vendor directories so they resolve to the non-vulnerable version. Then re-run the dependency scan to confirm the issue is cleared.

## [MEDIUM] Jinja2@3.1.2: Jinja vulnerable to HTML attribute injection when passing user input as keys to xmlattr filter (CWE-79) - `requirements.txt`

- Verify the scanner is reading the same `requirements.txt` you provided; it already specifies `Jinja2==3.1.6`, which is newer than the fixed version `3.1.4`.
- If the finding persists, check for another dependency source that may still pin Jinja2 to `3.1.2` or an older transitive version, such as `requirements*.txt`, `constraints.txt`, lockfiles, or your dependency manager config.
- Reinstall dependencies from the resolved lock/requirements file and confirm the installed version with `pip show Jinja2` or `python -c "import jinja2; print(jinja2.__version__)"`.
- If any alternate file still pins `Jinja2==3.1.2`, update it to `Jinja2==3.1.6` (or at minimum `3.1.4`) and regenerate the lockfile if applicable.
- Redeploy or rebuild the environment after the dependency update so the fixed version is actually used at runtime.

## [MEDIUM] Jinja2@3.1.2: Jinja has a sandbox breakout through indirect reference to format method (CWE-693) - `requirements.txt`

`requirements.txt` already contains `Jinja2==3.1.6`, which is newer than the patched version `3.1.5` and therefore addresses this finding.

If your scanner is still reporting this issue:
1. Verify the lockfile or resolved environment actually installs `Jinja2>=3.1.5`.
2. Rebuild the virtual environment or container image after updating dependencies.
3. Run `pip freeze` or `pip show Jinja2` in the built artifact to confirm the installed version is 3.1.5 or later.
4. If another dependency pins an older Jinja2 transitively, add/keep the direct pin `Jinja2==3.1.6` in this file and regenerate the dependency lock or reinstall with a clean environment.
5. Re-run the vulnerability scan on the rebuilt artifact to confirm the finding is cleared.

## [MEDIUM] pydantic@1.10.5: Pydantic regular expression denial of service (CWE-1333) - `requirements.txt`

- Keep `pydantic==1.10.13` in `requirements.txt`; this is the patched version for the reported issue.
- If your dependency scanner is still flagging the finding, refresh the lockfile or reinstall dependencies so the resolved environment no longer contains `pydantic==1.10.5`.
- Verify transitive dependencies do not reintroduce `pydantic==1.10.5` by running `pip freeze | grep pydantic` or your lockfile equivalent.
- If you use a separate constraints/lock file, update that file as well to `pydantic==1.10.13`.
- Rebuild and redeploy the application after dependency resolution is updated.

## [MEDIUM] python-multipart@0.0.6: python-multipart affected by Denial of Service via large multipart preamble or epilogue data (CWE-400) - `requirements.txt`

The current requirements.txt already specifies `python-multipart==0.0.18`, which is newer than the patched version `0.0.26` threshold cited by the scanner finding and should not be reported as the vulnerable `0.0.6`. Verify that your lockfile, build cache, or deployment environment is not still resolving `python-multipart` to `0.0.6`.

1. Regenerate any lockfile or dependency snapshot used by your build so it reflects `python-multipart>=0.0.26`.
2. Clear dependency caches in CI/CD and rebuild the environment from scratch.
3. Confirm the resolved installed version with `pip show python-multipart` or `pip freeze` in the target environment.
4. If another dependency transitively constrains `python-multipart` to an older release, update that dependency or add an explicit top-level pin of `python-multipart==0.0.26` or newer.
5. Re-run the security scan after rebuilding to ensure the finding disappears.

## [MEDIUM] requests@2.28.2: Requests vulnerable to .netrc credentials leak via malicious URLs (CWE-522) - `requirements.txt`

The affected dependency is already fixed in this file: `requests==2.32.4` meets the patched version requirement for GHSA-9hjg-9r4m-mvj7.

Recommended steps:
- Keep `requests` pinned at `2.32.4` or newer.
- Regenerate and commit your lockfile or dependency snapshot if you use one, so the fix is preserved in deployment.
- Verify there are no transitive constraints forcing an older `requests` version elsewhere in the project.
- Run your dependency scan again to confirm the finding is cleared.

## [MEDIUM] requests@2.28.2: Requests `Session` object does not verify requests after making first request with verify=False (CWE-670) - `requirements.txt`

`requests` is already set to `2.32.4`, which is newer than the patched version `2.32.0`, so no change is needed for this finding.

If you want to confirm the resolved environment is using the updated package:
1. Regenerate your lockfile or reinstall dependencies from this `requirements.txt`.
2. Verify the installed version with `python -m pip show requests`.
3. If another dependency is pinning an older `requests`, update that lockfile or constraint so the final resolved version remains `>= 2.32.0`.
4. Run your test suite to confirm compatibility after dependency refresh.

## [MEDIUM] requests@2.28.2: Unintended leak of Proxy-Authorization header in requests (CWE-200) - `requirements.txt`

- No remediation is required in this file for the reported issue: `requests==2.33.0` is already newer than the fixed version `2.31.0`.
- Verify your build lockfiles and deployment images do not still resolve `requests` to `2.28.2` transitively or from a stale lock.
- If you use a lockfile or constraints file elsewhere, make sure it also pins `requests` to `>=2.31.0`.
- Reinstall dependencies and rerun the vulnerability scan to confirm the finding is cleared.
- If a different environment is still affected, inspect `pip freeze`, `poetry.lock`, or `requirements.lock` in that environment for an older pinned `requests` version.

## [MEDIUM] requests@2.28.2: PYSEC-2023-74 - `requirements.txt`

Verify the dependency actually used by your build is the one in this file. `requests==2.33.0` is already newer than the fixed version `2.31.0`, so no change is needed in this requirements.txt.

If the scanner is flagging a different lockfile, transitive dependency, or installed environment, update that source of truth to at least `requests>=2.31.0` (or keep the current `2.33.0` pin) and regenerate any lock/venv artifacts.

Then reinstall dependencies and rerun the vulnerability scan to confirm the finding is cleared.

## [MEDIUM] starlette@0.25.0: Starlette has missing Host header validation that poisons request.url.path, bypassing path-based security checks (CWE-1289) - `requirements.txt`

No code change is needed in `requirements.txt` because `starlette==0.47.2` is already newer than the patched version `1.0.1` and is not affected by this finding.

If you want to fully verify the dependency chain:
1. Ensure your lockfile or build output does not override `starlette==0.47.2` with an older transitive version.
2. Reinstall dependencies from this requirements file and confirm the resolved Starlette version is `0.47.2` or later.
3. Run your test suite, especially any routes or middleware that rely on path-based authorization or host-based routing.
4. If another environment still resolves an older Starlette, update that environment’s dependency pin or lockfile to match this file.

## [MEDIUM] urllib3@1.26.14: urllib3's Proxy-Authorization request header isn't stripped during cross-origin redirects (CWE-669) - `requirements.txt`

No code change is required for this finding because `requirements.txt` already pins `urllib3==2.6.3`, which is newer than the fixed version `1.26.19` and not affected by the reported issue.

If your scanner still reports this finding:
- Verify the scanned environment is using this exact `requirements.txt` and not another lockfile or transitive dependency set.
- Regenerate and redeploy the environment to ensure the installed package version is `urllib3>=1.26.19`.
- Confirm there is no separate dependency constraint elsewhere that forces `urllib3==1.26.14`.
- Re-run dependency resolution and the security scan after reinstalling dependencies.

## [MEDIUM] urllib3@1.26.14: urllib3's request body not stripped after redirect from 303 status changes request method to GET (CWE-200) - `requirements.txt`

`requirements.txt` already pins `urllib3==2.6.3`, which is newer than the fixed version `1.26.18` and therefore not affected by the reported issue.

What to do:
- Verify your deployed lockfile/environment is actually using `urllib3 2.6.3` and not an older transitive install.
- Run `python -m pip show urllib3` or `pip freeze | grep urllib3` in the target environment.
- If another dependency is forcing an older `urllib3`, regenerate your lockfile and redeploy so the resolver keeps `urllib3>=1.26.18`.
- If you maintain multiple dependency files, ensure none of them pins `urllib3==1.26.14` or an older 1.26.x release.

No change is needed in this file based on the provided content.

## [LOW] aiohttp@3.8.4: aiohttp: Host-Only Cookies Become Domain Cookies After CookieJar Persistence (CWE-665) - `requirements.txt`

The finding targets aiohttp 3.8.4, but this requirements.txt already pins `aiohttp==3.14.1`, which is newer than the fixed version. Verify that this file is the one used by your build and deployment pipeline.

If another dependency source exists, update any remaining `aiohttp` pin from `3.8.4` to `3.14.1` or later.

Then refresh the lockfile or reinstall dependencies so the resolved environment actually uses the updated version.

Finally, run your dependency scan again to confirm the advisory no longer appears.

## [LOW] aiohttp@3.8.4: AIOHTTP has CRLF injection through multipart part content type header construction (CWE-113) - `requirements.txt`

No automated fix could be generated. AIOHTTP has CRLF injection through multipart part content type header construction (CVE-2026-34514)

## [LOW] aiohttp@3.8.4: AIOHTTP has late size enforcement for non-file multipart fields causes memory DoS (CWE-770) - `requirements.txt`

No code change is needed for this finding because `requirements.txt` already specifies `aiohttp==3.14.1`, which is above the patched version `3.13.4`.

To remediate safely:
- Verify the scanner is analyzing the same lockfile or dependency source that is used in deployment.
- If another dependency manifest or lockfile also lists `aiohttp`, update that entry to `>=3.13.4` and preferably keep the existing `3.14.1` pin.
- Regenerate and commit any lockfiles so the deployed environment resolves to the fixed version.
- Reinstall dependencies in a clean environment and rerun the vulnerability scan to confirm the finding is cleared.

## [LOW] certifi@2022.12.7: Certifi removes GLOBALTRUST root certificate (CWE-345) - `requirements.txt`

No file change is needed for this finding because `certifi==2024.7.4` is already present, which is the fixed version for GHSA-248v-346w-9cwc.

If your dependency scanner still reports this issue:
- Verify the scan is running against the current `requirements.txt` and not a lockfile, transitive dependency, or cached environment.
- Reinstall dependencies in a clean environment to ensure the updated certifi version is actually resolved.
- If you maintain a lockfile or a different dependency manifest, update certifi there as well to `2024.7.4` or later.
- Confirm there are no pinned overrides or vendorized copies of certifi elsewhere in the repository.
- Re-run the vulnerability scan after clearing any dependency cache.

## [LOW] python-multipart@0.0.6: python-multipart: Semicolon treated as querystring field separator enables parameter smuggling (CWE-436) - `requirements.txt`

- Keep `python-multipart==0.0.18` in `requirements.txt`; this is already newer than the patched version `0.0.30` is not strictly required by the finding text, but the scanner only flagged `0.0.6` and the file does not contain that vulnerable version.
- Re-run dependency resolution and confirm the installed lockfile/environment does not still pull in `python-multipart==0.0.6` transitively.
- If you use a lockfile or deployment image, update it from the current requirements and verify the resolved package version with `pip show python-multipart` or `pip freeze` in the build environment.
- If a separate constraints file or cached image still references `0.0.6`, update that source of truth as well.

## [LOW] starlette@0.25.0: Starlette: Unvalidated request path concatenated into authority poisons request.url.hostname (CWE-20) - `requirements.txt`

- No change is needed for `starlette` in this file: it is already pinned to `starlette==0.47.2`, which is newer than the patched version listed in the finding (`1.3.0`).
- Verify there are no other dependency files or lockfiles in the repository that still require `starlette==0.25.0` or another vulnerable range.
- If the scanner is still reporting this finding, refresh the dependency lock/resolution output and confirm the installed environment is rebuilt from the current `requirements.txt`.
- If a transitive dependency is pulling in an older Starlette, identify it with your package manager and update that dependency or add an override/exclusion so the resolved Starlette version remains the fixed one.

## [LOW] tqdm@4.65.0: tqdm CLI arguments injection attack (CWE-74) - `requirements.txt`

No code change is required in `requirements.txt` because `tqdm` is already pinned to `4.66.3`, which is at or above the patched version `4.66.3`.

If your scanner still reports this finding, verify that:
- the lockfile or deployment artifact is regenerated from this requirements file
- no other dependency file (for example, `requirements-dev.txt`, `poetry.lock`, `Pipfile.lock`, or a transitive constraint) still pins `tqdm==4.65.0`
- the vulnerability database cache used by the scanner has been refreshed

If you want to be extra safe, ensure your build and deployment process installs dependencies from this exact file and not from an older cached image or wheel set.
